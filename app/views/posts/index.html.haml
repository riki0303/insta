.container.post
  .head
    = link_to 'New Post', new_post_path, class: 'head__link head__link--blue'

  - @posts.each do |post|
    = image_tag post.user.profile.avatar
    %p
      = post.user.account
    %p
      = I18n.l(post.created_at, format: :default)
    - post.eyecatches.each do |eyecatch|
      = image_tag eyecatch
    .post__btn-container
      = image_tag 'unlike.png'
      = image_tag 'comment.png'
      = link_to image_tag('sshare.png'), 'https://twitter.com/share?'
    %p
      = post.title
