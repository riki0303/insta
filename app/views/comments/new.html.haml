.container.form
  .head
    .head__item
      = link_to :back do
        %span.arrow-left
    .head__item
      %h2.head__title
        Comment
    .head__item.head__item--right
  %ul
    - @comment.errors.full_messages.each do |message|
      %li= message

  .comment
    - @comments.each do |comment|
      -# 投稿用にbuildされた空のコメントを表示しないように条件分岐
      - unless comment.content.nil?
        .comment__item
          .comment__left
            = link_to profile_path do
              = image_tag comment.post.user.avatar_image, class: 'comment__avatar'
          .comment__right
            = link_to profile_path, class: 'comment__account' do
              = comment.post.user.account
            %p.comment__content
              = comment.content


  = form_with(model: @comment, url: post_comments_path(@comment.post), local: true) do |f|
    %div
      = f.text_area :content, class: 'form__input'
      .form__btn
        = f.submit "CREATE COMMENT", class: 'form__btn-text'